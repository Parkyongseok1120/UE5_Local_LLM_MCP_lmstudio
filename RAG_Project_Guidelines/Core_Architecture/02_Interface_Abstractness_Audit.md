# 인터페이스 함수의 필요성 검토

각 함수가 모든 구현체에 자연스러운지, 특정 구현체의 내부 기능인지, 없으면 계약이 성립하지 않는지 확인해야 합니다.

일부 구현체가 의미 없는 값을 돌려줘야 하거나 UI 편의 때문에 붙인 상세 조회라면 별도 조회·컴포넌트·작은 인터페이스로 분리합니다.

답변에는 남길 최소 함수, 제외한 함수와 이유, 필요할 때만 추가할 보조 계약을 적습니다. 구현하지 않은 선택 함수의 기본값 의미도 분명히 해야 합니다.
