STAGE 5 CONTINUATION — finish GameMode wiring. MCP only.

## Done already (do not redo)
- FGomokuMatchConfig.BlockedCells exists
- InitializeMatch applies BlockedCells via SetCellBlocked
- CreatePresetForSize updated

## Still missing
`AGomokuGameMode::ApplyBoardTemplate` is still a stub / BeginPlay still hardcodes 15x15.

## Required
1) Implement ApplyBoardTemplate():
   - If BoardTemplate valid: build FGomokuMatchConfig from template Width/Height/BlockedCells (+ MaxPlayers from DefaultHotseatPlayers, WinLength=5)
   - RuleEngine->InitializeMatch(Config)
   - If BoardActor exists: ApplyBoardSize + FitCameraToBoard (or whatever public API exists)
   - Sync GameState SetRuleEngineRef if needed
2) BeginPlay: call ApplyBoardTemplate() when BoardTemplate set; else keep default 15x15 path. Avoid double-init bugs.
3) RestartGame should re-apply template blocked cells (InitializeMatch already reapplies from MatchConfig — ensure template config is stored on RuleEngine).

Read GameMode.cpp/.h then replace_in_file. Stop when ApplyBoardTemplate is non-empty and BeginPlay uses it.
