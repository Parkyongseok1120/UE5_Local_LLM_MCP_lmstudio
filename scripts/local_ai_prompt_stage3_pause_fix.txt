Fix ONLY Tests/GomokuStage3TimeSystem.spec.cpp PauseStopsTick via MCP.

TickTimeSystem ignores DeltaSeconds for PersonalRemaining (only updates TurnElapsed from World GetTimeSeconds). In CreateWorld automation, world time often does not advance, so requiring TurnElapsed increase OR Personal decrease after unpause will flake/fail.

Replace the unpause progression block with:
- SetTimePaused(false) asserts bTimePaused false (keep)
- TickTimeSystem(1.0f) after unpause must not crash
- Assert PersonalRemaining still >= 0 and PlayerTimes valid
- Do NOT require TurnElapsed > 0.5 or Personal decrease

Keep paused-unchanged assertions strong (Personal and TurnElapsed equal before/after paused tick).

replace_in_file only. Stop when done.
