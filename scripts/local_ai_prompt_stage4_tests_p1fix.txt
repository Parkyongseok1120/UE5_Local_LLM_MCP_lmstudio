SUPERVISOR CODE REVIEW of your Part1 file `Source/O_Mock/Tests/GomokuStage4Behavior.spec.cpp` — FIX via MCP. Do not leave Spec/TEST_CASE as-is.

## Blocking issues
1) Includes from `Tests/` must be `#include "../GomokuRuleEngine.h"` (and GameState/Types). Flat includes will fail compile.
2) `BEGIN_DEFINE_SPEC` + `TEST_CASE(...)[ ... ];` is invalid UE automation syntax here. Replace the whole file approach with `IMPLEMENT_SIMPLE_AUTOMATION_TEST` per test (EditorContext | EngineFilter). Example pattern:
```
IMPLEMENT_SIMPLE_AUTOMATION_TEST(FName, "Gomoku.Stage4.TurnCycle2Players", EAutomationTestFlags::EditorContext | EAutomationTestFlags::EngineFilter)
bool FName::RunTest(const FString& Parameters) { ... return true; }
```
3) CollectTurnCycle must record `GetCurrentPlayerIndex()` (0-based), NOT `GetCurrentPlayerId()` (1-based). Expected [0,1,0,1] is indices.
4) Keep MakeEngine helper; MaxPlayers clamp already in engine.

## Action
- read_file the current Tests file
- rewrite via write_file (overwrite) with ONLY the 3 TurnCycle tests using IMPLEMENT_SIMPLE_AUTOMATION_TEST + GetCurrentPlayerIndex
- Then stop. Later parts will append more tests before `#endif`.
