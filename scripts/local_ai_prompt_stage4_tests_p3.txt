PART 3 — MCP-only. Append 5 remaining Stage 4 Automation tests to
`Source/O_Mock/Tests/GomokuStage4Behavior.spec.cpp` (before `#endif`).
Do NOT rewrite existing TurnCycle/Abandon/Skip/Reverse tests.

## Required exact Automation names (must match):
1) Gomoku.Stage4.RoundNoDuplicateCompletion
2) Gomoku.Stage4.RoundIncrementsOnlyAfterAllActive
3) Gomoku.Stage4.InactiveExcludedFromRound
4) Gomoku.Stage4.TimeSystem3And4Players
5) Gomoku.Stage4.Board21x21FourPlayers

Use IMPLEMENT_SIMPLE_AUTOMATION_TEST + RunTest like existing tests.
APIs (read headers with MCP first):
- UGomokuRuleEngine: MakeEngine pattern already in file; GetActivePlayerIndices; GetMatchConfig; IsValidEmpty; TryPlaceStone; AdvanceTurn; GetCurrentPlayerIndex; AbandonPlayer
- AGomokuGameState: SetRuleEngineRef; InitializeForLocalHotseat_Implementation (NOT the UFUNCTION wrapper); HandlePlaceStone; RequestAbandonCurrentPlayer_Implementation; CurrentRoundIndex; PlayersCompletedThisRound; LocalPlayerCount; PlayerTimes; MaxPersonalTime; MaxTurnTime; bHasTimeSystem; SetTimePaused; CurrentPlayerIndex
- World: UWorld::CreateWorld(EWorldType::Game,false); SpawnActor<AGomokuGameState>(); DestroyWorld(false)

## Behavior expectations (Stage 4 design)
A) RoundNoDuplicateCompletion: with MaxPlayers=3, model round completion as TSet membership of ALL active indices (not set.Num()==MaxPlayers). Adding index 0 twice must keep Num()==1. Complete only when Contains every ActivePlayerIndices entry.

B) RoundIncrementsOnlyAfterAllActive: World+GS+Engine MaxPlayers=3. InitializeForLocalHotseat_Implementation(3). After 1 place and after 2 places CurrentRoundIndex unchanged; after 3rd active player's place CurrentRoundIndex == start+1 and PlayersCompletedThisRound empty.

C) InactiveExcludedFromRound: 4 players. P0 places; then RequestAbandonCurrentPlayer_Implementation (abandons current=P1 without counting completion). Index 1 must leave active and must not stay in PlayersCompletedThisRound. Remaining active places until CurrentRoundIndex == start+1.

D) TimeSystem3And4Players: for Players in {3,4}: after InitializeForLocalHotseat_Implementation(Players), LocalPlayerCount==Players, PlayerTimes.Num()==Players, MaxPersonalTime==120, MaxTurnTime==25, bHasTimeSystem true, each PersonalRemaining==120; SetTimePaused true/false works.

E) Board21x21FourPlayers: MakeEngine(4,21). Config 21x21 MaxPlayers=4. IsValidEmpty(0,0) and (20,20) true; (21,0) false. Place stones for PlayerIds 1..4 on consecutive turns with AdvanceTurn; indices cycle 0→1→2→3→0.

read_file_range end of file, then replace_in_file to insert before #endif. Prefer multiple small patches. Stop when all 5 names exist.
