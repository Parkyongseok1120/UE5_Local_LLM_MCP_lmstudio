BUILD FAIL — fix test only via MCP.

## Errors
```
GomokuStage6ItemFramework.spec.cpp(99): error C3861: 'CreateWorld': identifier not found
GomokuStage6ItemFramework.spec.cpp(143): error C3861: 'DestroyWorld': identifier not found
```

## Fix (match Stage4/5 tests)
Replace:
```
UWorld* World = CreateWorld(EWorldType::Game, FName(...));
...
DestroyWorld(*World);
```
With:
```
UWorld* World = UWorld::CreateWorld(EWorldType::Game, false);
...
World->DestroyWorld(false);
```

Also add `#include "GomokuItemTypes.h"` if needed. replace_in_file only on the test file. Stop when fixed.
