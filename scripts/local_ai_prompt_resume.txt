SUPERVISOR: LM 400 infra fixed. Resume Stage 4 GAME CODE work on O-Mock via MCP. Same campaign session file.

## Already fixed earlier (do not revert)
- AGomokuPlayerState: PlayerId renamed to GomokuPlayerId (UHT shadowing)
- RuleEngine: removed bogus IsValidPlayerId call
- GameState: removed GetLifetimeReplicatedProps / DOREPLIFETIME for local hotseat
- MatchEventLog: LastOrDefault replaced
- HUD: DrawHUD + OnTickPlayerTime signature/impl present
- PlayerController.h: OnMouseMoveX declared; EngineUtils include present

## Still failing / incomplete — diagnose with MCP reads, then patch
Compile/link likely still broken:
1) GomokuPlayerController.cpp still calls ClearHoveredCell() which does not exist on AGomokuGameState
2) GetMousePosition usage may still be wrong (needs float out-params)
3) OnMouseMoveY may be used without a header declaration
4) AGomokuGameMode::ApplyBoardTemplate declared but undefined (Stage 4 does not need board templates — remove or stub)
5) RequestAbandonCurrentPlayer is declared (Server Reliable) but Implementation is missing

Stage 4 logic review (build green alone is not pass):
A) GomokuGameMode BeginPlay hardcodes MaxPlayers=2 / InitializeForLocalHotseat(2). Need same path for 2/3/4.
B) EndCurrentTurn uses PlayersCompletedThisRound.Num() >= ActiveCount — unsafe with abandon; must require every active index is contained in the set.
C) Preserve Stage 3 time system (personal/turn timers, recovery, timeout auto-move, bTimePaused).
D) Do not expand Stage 5+ features this turn.

Read the failing/related files, apply bounded MCP mutations, then summarize what you changed and how abandon + round completion work.
