BUILD FAIL (C1083) — MCP-only fix on O-Mock.

## Compiler error
`Source/O_Mock/Tests/GomokuStage4Behavior.spec.cpp(6): fatal error C1083: 'GomokuRuleEngine.h': No such file or directory`

Files under `Source/O_Mock/Tests/` do NOT see parent module headers via bare names.

## Required fix
In `Source/O_Mock/Tests/GomokuStage4Behavior.spec.cpp` change includes to:
```
#include "Misc/AutomationTest.h"
#include "Engine/World.h"
#include "../GomokuRuleEngine.h"
#include "../GomokuGameState.h"
#include "../GomokuTypes.h"
```

Optional alternative (also allowed): add `PrivateIncludePaths.Add(ModuleDirectory);` in `Source/O_Mock/O_Mock.Build.cs` — but prefer fixing the test includes first.

Do NOT change game logic. replace_in_file the includes. Stop when done.
