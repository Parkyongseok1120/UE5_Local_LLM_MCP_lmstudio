STAGE 6 — Item Framework (base only). MCP-only. Do NOT implement the five concrete item effects (Stage 7). Keep RuleEngine place/win mostly untouched.

## Required deliverables
1) Create `Source/O_Mock/GomokuItemTypes.h` with:
   - EItemTargetType (None, Cell, Player, Self at minimum)
   - EGomokuItemType enum (placeholder values OK; Stage7 fills effects)
   - FItemData { Id, DisplayName, EnergyCost, TargetType }

2) Expand `Source/O_Mock/GomokuItemLibrary.h` and add `GomokuItemLibrary.cpp`:
   - static/API: CanUseItem(PlayerId, ItemId, …), ValidateTarget(…), ExecuteItem(…) SEPARATE functions
   - ExecuteItem may be stub that returns false / no-op for unknown items but MUST exist as distinct API
   - Energy cost / inventory hooks: read FGomokuPlayerStateData::ItemIds if present

3) PlayerController + HUD placeholders:
   - SelectItem / CancelItemTargeting (or equivalent) targeting mode flags
   - HUD: item slot / targeting mode display placeholders (no fancy UI required)

## Constraints
- Read existing GomokuItemLibrary.h, GomokuTypes.h, PlayerController, HUD first
- Allowed paths include Types/RuleEngine/GameMode/GameState/BoardActor/Build.cs/PC/HUD
- Small replace_in_file / write_file patches
- After APIs exist, also create `Source/O_Mock/Tests/GomokuStage6ItemFramework.spec.cpp` with Automation:
  - Gomoku.Stage6.ItemTypesExist (compile-time via constructing FItemData + TargetType)
  - Gomoku.Stage6.CanUseValidateExecuteSeparated (call all three; stub Execute ok; assert CanUse false without item / true when ItemIds contains id if you implement that)
  - Gomoku.Stage6.SelectCancelTargeting (PC SelectItem then Cancel clears targeting flag)

Stop when framework APIs + 3 tests exist and look compile-ready.
