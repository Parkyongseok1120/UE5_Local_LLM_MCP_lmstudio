PART 3b FIX — MCP only on Source/O_Mock/Tests/GomokuStage4Behavior.spec.cpp.
Do not change game code. Fix compile/API bugs in the 5 new tests.

## Must fix
1) `CreateWorld(...)` is WRONG. Use `UWorld::CreateWorld(EWorldType::Game, false)` everywhere.
2) `TryPlaceStone` signature is `(int32 PlayerId, int32 X, int32 Y)` — NOT FIntPoint. Fix Board21 test:
   `Engine->TryPlaceStone(PlayerId, i, i);`
3) Fix broken Printf: `FString::Printf(TEXT("TryPlaceStone for PlayerId %d"), PlayerId)` — no `*PlayerId`.
4) InactiveExcludedFromRound: after abandon, do NOT hardcode HandlePlaceStone(2)/(3) if CurrentPlayerIndex may differ. Loop like:
   while (CurrentRoundIndex==StartRound && guard<8) { Cur=GS->CurrentPlayerIndex; GS->HandlePlaceStone(Cur, FIntPoint(guard,1)); }
   Ensure index 1 stays out of active/completed.
5) RoundIncrements: prefer place via `GS->CurrentPlayerIndex` each time (not hardcoded 0,1,2) in case StartNewTurn already advanced — OR keep hardcoded only if you first assert CurrentPlayerIndex matches.

read headers if unsure. replace_in_file until CreateWorld/TryPlaceStone/Printf issues are gone. Stop when compile-ready.
