STAGE 3 test FIX — compile blockers. MCP only on the test file (prefer) or GameState.h if you must expose APIs.

## Problem
`GomokuStage3TimeSystem.spec.cpp` calls `GS->TickTimeSystem` and `GS->ApplyEndOfRoundRecovery`, but both are **private** in GomokuGameState.h. This will not compile.

## Fix options (pick one coherent approach)
A) Preferred for tests: In GomokuGameState.h move `TickTimeSystem` and `ApplyEndOfRoundRecovery` to **public** (or protected + friend test — public is fine for this project). Keep AutoMoveOnTimeout private if unused by tests.
B) Or rewrite tests to only use public surface: SetTimePaused, Tick via World/GameState Tick if IsGameActive, and trigger recovery through real EndCurrentTurn round completion (harder).

Also verify InitializeForLocalHotseat_Implementation exists and sets IsGameActive / PlayerTimes (PauseStopsTick needs TickTimeSystem to run — read TickTimeSystem: it returns early if !IsGameActive).

Read GomokuGameState.cpp TickTimeSystem before changing. After making TickTimeSystem public, PauseStopsTick should work if IsGameActive true after InitializeForLocalHotseat.

Bounded patches. Stop when Stage3 tests compile against public/visible APIs.
