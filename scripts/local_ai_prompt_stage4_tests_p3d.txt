BUILD FAIL — MCP fix test file only (prefer), or add tiny getter if needed.

## Error
`GomokuStage4Behavior.spec.cpp(190): error C2039: 'GetTurnDirection': is not a member of 'UGomokuRuleEngine'`

## Fix
Read `Source/O_Mock/GomokuRuleEngine.h`. Use the actual public API for turn direction (likely a public `TurnDirection` field, not GetTurnDirection).

In ReverseTurnOrder test replace:
`Engine->GetTurnDirection()` → whatever exists (e.g. `Engine->TurnDirection`).

If there is truly no public accessor, you MAY add a one-line `int32 GetTurnDirection() const { return TurnDirection; }` to RuleEngine.h — minimal.

Do not change game turn logic. Stop when compile-ready.
