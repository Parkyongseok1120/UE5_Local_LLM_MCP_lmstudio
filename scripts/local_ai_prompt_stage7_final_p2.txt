STAGE 7 FINAL p2 — FRESH SESSION. Prior replace_in_file loop failed. Prefer write_file for whole files. MCP only. O-Mock.

## Step A — RuleEngine.h
Add PUBLIC setter near GetLastItemWinResult:
```
void SetLastItemWinResult(const FGomokuWinResult& InResult) { LastItemWinResult = InResult; }
```
LastItemWinResult already exists private. GetLastItemWinResult already exists. Do NOT leave LastItemWinResult without a setter ItemLibrary can call.

## Step B — write_file entire Source/O_Mock/GomokuItemLibrary.cpp
Read current file once, then write_file COMPLETE replacement implementing:

ValidateTarget updates:
- Pull: Target empty + exists orthogonal adjacent opponent stone (CellStateToPlayerId, owner!=PlayerId needed? Pull moves stone — use opponent-only) not guardian-protected
- Steal: occupied, owner!=PlayerId, !IsCellGuardianProtected, not Blocked

ExecuteItem (consume-LAST):
1. Def, IsItemUsableNow, ValidateTarget — fail return false
2. switch apply effect WITHOUT energy yet
3. SealStone SetCellBlocked; SkipTurn SetPlayerSkipNextTurn(TargetPlayerIndex+1);
4. Steal: ChangeCellOwnership; SetLastItemWinResult(CheckWinAt(Target))
5. Pull: find first NESW adj opponent stone !guardian; Owner=CellStateToPlayerId; RemoveStoneAt; ForcePlaceStone(Owner, Target); clear guardian on src if API exists; SetLastItemWinResult(CheckWinAt(Target))
6. GuardianBarrier SetCellGuardianProtected
7. On effect success ONLY: ConsumePlayerEnergy; RemoveItemFromInventory; SetUsedItemThisTurn; return true
8. On effect fail: return false with NO consume

Keep IsRegisteredItemId / CanUseItem / GetStaticItemDefs as now (comment Pull not stubbed).

## Step C — Append tests to GomokuStage7Items.spec.cpp via write_file FULL file
Keep existing 4 tests + MakeStage7Engine. ADD:
- PullMovesAdjacentStone
- PullFailsLeavesStateUnchanged  
- StealBlockedByGuardian
- StealRechecksWin (ForcePlace 4 stones for P1 in a row, opponent stone completing cell, Steal that cell → GetLastItemWinResult.IsWin or CheckWinAt)
- SecondItemUseRejectedSameTurn

Stop after write_file succeeds for cpp + tests. Summarize paths.
