SUPERVISOR FEEDBACK TURN 2 — same session. GAME_CODE_FAILED. Fix via MCP only. Stage 4 scope.

## Confirmed fixed
- `GomokuPlayerState.h`: `PlayerId` shadowing is GONE (now `GomokuPlayerId`). Do not revert.

## Fresh Unreal build FAILED (exit 6). Fix these compile errors first
Read each file with MCP before patching. Bounded replace_in_file only (newText <= 60 lines).

1) `GomokuRuleEngine.cpp:272` — `FGomokuRuleEngine::IsValidPlayerId` does not exist (wrong class prefix too; class is `UGomokuRuleEngine`). Add a tiny helper or delete the call and rely on active/abandoned/skip checks.

2) `GomokuGameState.h/.cpp` — `GetLifetimeReplicatedProps` override does not match UE 5.8 base (`C3668`/`C2664`). For Stage 4 local hotseat, PREFER removing the override + DOREPLIFETIME block and `Replicated` specifiers rather than inventing network code. Stage 9 handles net.

3) `GomokuMatchEventLog.h:35` — `TArray::LastOrDefault` does not exist. Use `Events.Num()>0 ? Events.Last() : FMatchEvent{}` (or equivalent).

4) `GomokuHUD.h/.cpp` — `Draw` is not an AHUD override. Use `virtual void DrawHUD() override`. Fix canvas drawing (`Canvas` deref, font pointer, no protected FCanvasItem::Color misuse). Keep HUD minimal for Stage 4 (player color/order/time placeholders).

5) `GomokuPlayerController.cpp` — multiple API breaks:
   - missing `#include "EngineUtils.h"` for `TActorIterator`
   - mouse move handlers `OnMouseMoveX/Y` undeclared — declare or remove
   - `GetMousePosition` needs float& out params, not zero-arg
   - `ClearHoveredCell` does not exist on GameState — use `SetHoveredCell(FIntPoint(-1,-1))` or add that one helper
   Keep Stage 3 hover→`SetHoveredCell` behavior.

6) Missing definitions (will fail link after compile):
   - Implement `AGomokuGameState::RequestAbandonCurrentPlayer_Implementation` using `RuleEngine->AbandonPlayer`, sole-active win, and remove abandoned indices from `PlayersCompletedThisRound`.
   - `ApplyBoardTemplate` declared but undefined — Stage 4 does NOT need it: REMOVE declaration (+ BoardTemplate UPROPERTY if unused) OR add empty stub. Prefer remove.

## Stage 4 logic (required even after green build)
A) `GomokuGameMode.cpp` hardcodes `MaxPlayers=2` / `InitializeForLocalHotseat(2)`. Add `UPROPERTY EditDefaultsOnly int32 DefaultHotseatPlayers = 2` clamped 2..4; same path for 2/3/4.
B) `EndCurrentTurn`: replace `PlayersCompletedThisRound.Num() >= ActiveCount` with membership check: every index in `GetActivePlayerIndices()` is contained in `PlayersCompletedThisRound`. Then clear set and increment `CurrentRoundIndex`. Inactive/abandoned must not count.
C) Do not break Stage 3 timers (`TickTimeSystem`, recovery, timeout auto-move, `bTimePaused`).
D) Do not expand minigame/lobby/items further this turn.

## Workflow
1. read_file the failing headers/cpps above
2. patch until mutations land
3. summarize files changed + how abandon + round completion work now
4. stop (supervisor will rebuild)
