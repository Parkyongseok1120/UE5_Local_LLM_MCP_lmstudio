CONTINUE same session. Unreal build FAILED again (exit 6). Fix via MCP reads + bounded replace_in_file. Do not invent APIs.

## Compile errors (exact)
1) GomokuGameMode.cpp(82): ApplyBoardTemplate(int32) does not match header ApplyBoardTemplate() — declaration/definition mismatch.
2) GomokuGameState.cpp(168): UWorld has no IsNetDedicatedServer — wrong API for authority/standalone gate.
3) GomokuGameState.cpp(365): EMatchEventType::PlayerLeft does not exist — use an existing enum value from GomokuMinigameTypes/MatchEventLog (read the enum).
4) GomokuHUD.cpp(123): AGomokuGameState::GetNumActivePlayers does not exist.
5) GomokuPlayerController.cpp(24): TActorIterator has no IsValid() — use standard iterator idiom (!It).
6) GomokuPlayerController.cpp(47): UInputComponent::BindKey cannot take a lambda — bind a UFUNCTION member instead.

## Stage 4 logic review (must fix; not compile-only)
A) RequestAbandonCurrentPlayer_Implementation calls EndCurrentTurn(true) after AbandonPlayer. EndCurrentTurn adds CurrentPlayerIndex into PlayersCompletedThisRound — abandoned players must NOT count as round completion. Expected: remove abandoned index from the set (and/or skip Add for abandoned), then advance to next active without treating abandon as a completed action.
B) BeginPlay still effectively defaults to 2 players via LocalPlayerCount default. Prefer an EditDefaultsOnly DefaultHotseatPlayers (2..4) on GameMode driving InitializeForLocalHotseat for the same 2/3/4 path.
C) Keep Stage 3 timers intact.
D) Avoid expanding Stage 5+ beyond compiling stubs.

Read the failing files, patch, summarize. Supervisor will rebuild.
