CONTINUE Stage 4. Apply ALL items below with MCP. read_file_range immediately before each replace_in_file. newText <= 40 lines. Prefer many small patches.

1) Source/O_Mock/GomokuMatchEventLog.h GetLastEvent:
Replace `return Events.LastOrDefault(Empty);` with:
```
if (Events.Num() == 0) { return Empty; }
return Events.Last();
```

2) Source/O_Mock/GomokuHUD.h:
- `virtual void Draw() override;` -> `virtual void DrawHUD() override;`
- Change OnTickPlayerTime to:
  `void OnTickPlayerTime(int32 PlayerIndex, const FGomokuPlayerTimeState& TimeState);`

3) Source/O_Mock/GomokuHUD.cpp:
- Rename Draw to DrawHUD
- Fix AddDynamic/OnTickPlayerTime binding to match new signature
- If canvas item code fails compile, simplify DrawHUD to Super::DrawHUD(); plus DrawText for CurrentTurnText and PlayerTimeStrings only

4) Source/O_Mock/GomokuPlayerController.h: declare if missing:
  void OnMouseMoveX(float Value); void OnMouseMoveY(float Value);
  And ensure .cpp includes "EngineUtils.h"
  Replace ClearHoveredCell() call with SetHoveredCell(FIntPoint(-1,-1))
  Fix GetMousePosition to use float X,Y out-params

5) Source/O_Mock/GomokuGameMode.h:
- Remove BoardTemplate UPROPERTY, ApplyBoardTemplate decl, and GomokuBoardTemplateDataAsset include if unused
- Add: UPROPERTY(EditDefaultsOnly, Category="Gomoku") int32 DefaultHotseatPlayers = 2;

6) Source/O_Mock/GomokuGameMode.cpp BeginPlay:
Use clamped DefaultHotseatPlayers (2..4) for Config.MaxPlayers and InitializeForLocalHotseat(...)

7) Source/O_Mock/GomokuGameState.cpp EndCurrentTurn:
Replace `bool bRoundCompleted = ActiveCount > 0 && PlayersCompletedThisRound.Num() >= ActiveCount;`
with membership loop over GetActivePlayerIndices() Contains check.

8) Append RequestAbandonCurrentPlayer_Implementation at end of GomokuGameState.cpp before last functions is fine:
- abandon CurrentPlayerIndex via RuleEngine->AbandonPlayer(CurrentPlayerIndex+1)
- Remove that index from PlayersCompletedThisRound
- sole active => win + IsGameActive=false + OnMatchEnded
- else sync CurrentPlayerIndex from engine and StartNewTurn()

Do not touch GomokuPlayerState. Stop and summarize.
