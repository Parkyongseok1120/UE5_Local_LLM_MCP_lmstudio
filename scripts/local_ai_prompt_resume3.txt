CONTINUE same session. Unreal BUILD SUCCEEDED and Stage 4 static signature verify passed, but Stage 4 LOGIC REVIEW FAILS — do not stop.

## Blocking logic bug
File: `Source/O_Mock/GomokuGameState.cpp`
Function: `RequestAbandonCurrentPlayer_Implementation`

Current sequence:
1) `PlayersCompletedThisRound.Remove(CurrentPlayerIndex);`
2) `EndCurrentTurn(true);`

But `EndCurrentTurn` always does `PlayersCompletedThisRound.Add(CurrentPlayerIndex);` near the top.

Symptom: abandoned player is re-added and can still satisfy round completion.
Expected: abandon must NOT count as a completed action for the round. Options (choose a correct one after reading both functions):
- advance turn without going through EndCurrentTurn's Add path, OR
- teach EndCurrentTurn a flag that skips Add for abandon, OR
- remove again after EndCurrentTurn and recompute round completion without the abandoned index (fragile).

Also purge any already-completed abandoned indices when active set changes.

## Remaining Stage 4 requirement gap
`GomokuGameMode::BeginPlay` still derives MaxPlayers from `GS->LocalPlayerCount` which defaults to 2. Add `EditDefaultsOnly int32 DefaultHotseatPlayers = 2` on GameMode (clamp 2..4) and drive `InitializeForLocalHotseat` / Config.MaxPlayers from that so 2/3/4 share one path without requiring a BP override.

Read the two functions, patch with MCP, summarize. Do not expand Stage 5+.
