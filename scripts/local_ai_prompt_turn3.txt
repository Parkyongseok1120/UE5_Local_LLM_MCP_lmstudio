CONTINUE same session. Prior turn partially applied then LM 400 aborted. Finish remaining compile+Stage4 fixes. One bounded replace_in_file at a time. ALWAYS read_file_range the exact lines right before each patch. newText <= 40 lines.

## Already done (do not undo)
- RuleEngine AdvanceTurn: `IsValidPlayerId` removed; uses `NextPlayerId <= 0`
- GameState.h: removed `GetLifetimeReplicatedProps` declaration and Replicated UPROPERTY flags

## MUST still fix (exact targets)

### 1) GomokuGameState.cpp — DELETE GetLifetimeReplicatedProps body (lines ~19-29)
Remove the whole function. Keep constructor. Also remove `#include "Net/UnrealNetwork.h"` if unused.

### 2) GomokuMatchEventLog.h GetLastEvent — replace LastOrDefault
Replace body with:
```
static FMatchEvent Empty;
if (Events.Num() == 0) { return Empty; }
return Events.Last();
```

### 3) GomokuHUD.h — change `virtual void Draw() override;` to `virtual void DrawHUD() override;`
Also fix OnTickPlayerTime signature to match GameState delegate:
`void OnTickPlayerTime(int32 PlayerIndex, const FGomokuPlayerTimeState& TimeState);`

### 4) GomokuHUD.cpp — rename Draw→DrawHUD; fix canvas usage
- Use DrawHUD()
- Include what you need; if canvas drawing is broken, simplify DrawHUD to only DrawText for CurrentTurnText / times (minimal Stage 4 HUD). Do not invent GetWhiteBrush if unavailable.

### 5) GomokuPlayerController.h/.cpp
- Include EngineUtils.h if using TActorIterator
- Fix GetMousePosition(float& X, float& Y)
- Remove OnMouseMoveX/Y if undeclared OR declare private handlers in .h
- Replace ClearHoveredCell(...) with SetHoveredCell(FIntPoint(-1,-1))

### 6) GomokuGameMode.h/.cpp Stage 4 path
- REMOVE ApplyBoardTemplate declaration and BoardTemplate UPROPERTY (and include of BoardTemplateDataAsset if unused)
- ADD: `UPROPERTY(EditDefaultsOnly) int32 DefaultHotseatPlayers = 2;`
- BeginPlay: clamp DefaultHotseatPlayers to 2..4; set Config.MaxPlayers; call InitializeForLocalHotseat(that)

### 7) GomokuGameState.cpp EndCurrentTurn round check
Replace `PlayersCompletedThisRound.Num() >= ActiveCount` with:
```
bool bRoundCompleted = false;
if (ActiveCount > 0) {
  bRoundCompleted = true;
  for (int32 Idx : RuleEngine->GetActivePlayerIndices()) {
    if (!PlayersCompletedThisRound.Contains(Idx)) { bRoundCompleted = false; break; }
  }
}
```

### 8) Add RequestAbandonCurrentPlayer_Implementation in GomokuGameState.cpp
- If inactive/invalid return
- PlayerId = CurrentPlayerIndex+1; RuleEngine->AbandonPlayer(PlayerId)
- PlayersCompletedThisRound.Remove(CurrentPlayerIndex)
- If GetSoleActivePlayerIndex()!=INDEX_NONE → set winner, IsGameActive=false, broadcast end
- Else EndCurrentTurn(true) or StartNewTurn after syncing CurrentPlayerIndex from engine

Stop after these mutations and summarize.
