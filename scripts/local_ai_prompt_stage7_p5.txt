STAGE 7 Part5 — TESTS ONLY. Build already Succeeds. MCP write_file.

Create `Source/O_Mock/Tests/GomokuStage7Items.spec.cpp`:

Helpers: NewObject RuleEngine InitializeMatch 2p 15.

1) Gomoku.Stage7.SealStoneBlocksCell
   AddItemToInventory(1,1) WITHOUT MarkItemGainedThisTurn; give energy if needed (AddPlayerEnergy);
   ExecuteItem SealStone on (5,5); TestFalse IsValidEmpty(5,5); GetCellState Blocked.

2) Gomoku.Stage7.SkipTurnFlagsPlayer
   AddItem(1,4); ExecuteItem with TargetPlayerIndex=1 (player id 2); Test player2 bSkipNextTurn via GetPlayerStateData(2).

3) Gomoku.Stage7.NewItemNotUsableSameTurn
   AddItem(1,1); MarkItemGainedThisTurn(1,1); TestFalse CanUseItem; ClearTurnItemLocksForPlayer(1); TestTrue CanUseItem.

4) Gomoku.Stage7.EnergyCostConsumed
   Set energy known via Consume/Add; Execute SealStone; assert Energy decreased by cost 1.

Use UWorld::CreateWorld only if needed (prefer RuleEngine-only). Includes like Stage6. Stop when 4 names exist.
