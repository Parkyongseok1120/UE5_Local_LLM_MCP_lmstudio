Create Stage 4 Unreal Automation tests via MCP ONLY. O-Mock game-project code.

## Goal
Create NEW file: `Source/O_Mock/Tests/GomokuStage4Behavior.spec.cpp`

Do NOT copy any supervisor draft (there is none in the project). Author the tests yourself after reading RuleEngine/GameState APIs.

## Read first (MCP)
- Source/O_Mock/GomokuRuleEngine.h
- Source/O_Mock/GomokuGameState.h (and relevant .cpp sections via read_file_range)
- Confirm PlayerId vs index: PlayerId = index+1; CurrentPlayerIndex is 0-based into Players/PlayerOrder.

## File requirements
- `#if WITH_DEV_AUTOMATION_TESTS` ... `#endif`
- Includes from Tests/: `#include "../GomokuRuleEngine.h"`, `../GomokuGameState.h`, `../GomokuTypes.h`, plus AutomationTest.h, Engine/World.h
- Flags: `EAutomationTestFlags::EditorContext | EAutomationTestFlags::EngineFilter`

## Required test paths (exact names)
1. Gomoku.Stage4.TurnCycle2Players — MaxPlayers=2; AdvanceTurn x4 → indices [0,1,0,1]
2. Gomoku.Stage4.TurnCycle3Players — MaxPlayers=3; x6 → [0,1,2,0,1,2]
3. Gomoku.Stage4.TurnCycle4Players — MaxPlayers=4; x8 → [0,1,2,3,0,1,2,3]
4. Gomoku.Stage4.AbandonExcludesPlayer — 4p AbandonPlayer(2); active excludes index 1; from 0 Advance → 2→3→0
5. Gomoku.Stage4.SkipNextPlayer — 4p SetPlayerSkipNextTurn(2) while current=0; first AdvanceTurn must land on index 2 (because id2==index1 is skipped when becoming current). Comment this mapping in code.
6. Gomoku.Stage4.ReverseTurnOrder — index=1, ReverseTurnDirection; Advance → 0 then 3
7. Gomoku.Stage4.RoundNoDuplicateCompletion — TSet Add(0) twice Num==1; membership complete only when all active present
8. Gomoku.Stage4.RoundIncrementsOnlyAfterAllActive — spawn World+GameState, InitializeForLocalHotseat_Implementation(3), HandlePlaceStone for each; CurrentRoundIndex +1 only after 3rd active act; set cleared
9. Gomoku.Stage4.InactiveExcludedFromRound — 4p; one place; RequestAbandonCurrentPlayer_Implementation; abandoned not in completed; remaining places until round+1
10. Gomoku.Stage4.TimeSystem3And4Players — hotseat 3 and 4: PlayerTimes.Num==N, MaxPersonalTime≈120, MaxTurnTime≈25 (use FMath::IsNearlyEqual)
11. Gomoku.Stage4.Board21x21FourPlayers — board 21x21, MaxPlayers=4; IsValidEmpty far corner; place+Advance through 4 players back to 0

## Implementation tips
- Helper NewObject<UGomokuRuleEngine> + InitializeMatch config
- GameState tests: UWorld::CreateWorld, SpawnActor<AGomokuGameState>, DestroyWorld
- Call `*_Implementation` for Server RPCs in tests
- write_file the full new .spec.cpp (create Tests/ directory by writing the file path)

After mutation, list written path and confirm SkipNextPlayer expectation in your summary.
