[pytest]
markers =
    smoke: optional local-disk regression (Project_MJS, etc.)
    slow: integration tests that may take minutes or require UBT

testpaths =
    tests

addopts =
    -m "not smoke and not slow"
