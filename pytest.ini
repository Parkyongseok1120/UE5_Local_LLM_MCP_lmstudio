[pytest]
markers =
    smoke: optional local-disk regression tests that require a real UE project on disk
    slow: integration tests that may take minutes or require UBT

testpaths =
    tests

addopts =
    -m "not smoke and not slow"
