{
  "_comment": "Example MCP config for lmstudio-unreal-agent-mcp. Copy relevant entries to %USERPROFILE%\\.lmstudio\\mcp.json. ALLOW_WRITE/COMMANDS/UNREAL_BUILD default to 0 (read-only safe mode). Enable only for trusted projects via installer\\Enable-AgentMode.ps1.",
  "mcpServers": {
    "unreal-agent": {
      "command": "node",
      "args": [
        "%USERPROFILE%\\.lmstudio\\UE5_Local_LLM_MCP_lmstudio\\lmstudio-unreal-agent-mcp\\src\\server.js"
      ],
      "env": {
        "WORKSPACE_ROOT": "%USERPROFILE%\\Documents",
        "ALLOW_WRITE": "0",
        "ALLOW_COMMANDS": "0",
        "ALLOW_UNREAL_BUILD": "0",
        "MAX_READ_BYTES": "524288",
        "MAX_OUTPUT_BYTES": "262144",
        "COMMAND_TIMEOUT_MS": "600000"
      }
    }
  }
}
