{
  "_comment": "Default Direct Model Mode example. The root installer manages %USERPROFILE%\\.lmstudio\\mcp.json. Reads are task-free; ALLOW_WRITE/COMMANDS/UNREAL_BUILD default to 0. write_file is create-only and replace_in_file requires the current SHA-256. Pass project on individual calls when one process serves multiple Unreal projects.",
  "mcpServers": {
    "unreal-agent": {
      "command": "node",
      "args": [
        "%USERPROFILE%\\.lmstudio\\UE5_Local_LLM_MCP_lmstudio\\lmstudio-unreal-agent-mcp\\src\\direct-server.js"
      ],
      "env": {
        "WORKSPACE_ROOT": "%USERPROFILE%\\Documents",
        "SHARED_UNREAL_CONFIG": "%USERPROFILE%\\.lmstudio\\config\\unreal-workspace.json",
        "AGENT_STATE_ROOT": "%USERPROFILE%\\.lmstudio\\state\\unreal-agent",
        "ALLOW_WRITE": "0",
        "ALLOW_COMMANDS": "0",
        "ALLOW_UNREAL_BUILD": "0",
        "MAX_READ_BYTES": "524288",
        "MAX_OUTPUT_BYTES": "262144",
        "COMMAND_TIMEOUT_MS": "600000"
      }
    }
  }
}
